import { Type, Injector } from '@angular/core';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { TdHttpService } from '../interceptors/http.service';
import { TdHttpRESTResponseType, TdHttpRESTObserve } from '../http.interfaces';
export interface ITdHttpRESTConfig {
    baseHeaders?: HttpHeaders;
    baseUrl: string;
    defaultObserve?: TdHttpRESTObserve;
    defaultResponseType?: TdHttpRESTResponseType;
}
declare type Constructor<T> = new (...args: any[]) => T;
export declare function getInjector(): Injector;
/**
 * Mixin to augment a service with http helpers.
 * @internal
 */
export declare function mixinHttp(base: any, config: ITdHttpRESTConfig, httpInject?: Type<HttpClient | TdHttpService>): Constructor<any>;
/**
 * @internal
 * WORKAROUND until Ivy Renderer is ready
 */
export declare class InternalHttpService {
    static _injector: Injector;
    constructor(_injector: Injector);
}
export {};
