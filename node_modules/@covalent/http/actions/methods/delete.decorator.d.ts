import { ITdHttpRESTOptions } from '../../';
/**
 * Decorator that adds DELETE request capabilities to a method
 */
export declare function TdDELETE(config: {
    path: string;
    options?: ITdHttpRESTOptions;
}): Function;
