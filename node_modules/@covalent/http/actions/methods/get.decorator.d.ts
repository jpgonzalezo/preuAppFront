import { ITdHttpRESTOptions } from '../../';
/**
 * Decorator that adds GET request capabilities to a method
 */
export declare function TdGET(config: {
    path: string;
    options?: ITdHttpRESTOptions;
}): Function;
