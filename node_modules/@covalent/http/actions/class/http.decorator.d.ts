import { ITdHttpRESTConfig } from '../http.mixin';
/**
 * Decorator used to give a service http capabilities using TdHttpService
 */
export declare function TdHttp(config: ITdHttpRESTConfig): Function;
/**
 * Decorator used to give a service http capabilities using HttpClient
 */
export declare function TdHttpClient(config: ITdHttpRESTConfig): Function;
